{
  "name": "rate-limiter-server",
  "version": "1.0.0",
  "description": "Backend API for the Rate Limiter SDK",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "tsc && node dist/index.js",
    "build": "tsc"
  },
  "dependencies": {
    "express": "^4.18.2",
    "redis": "^4.6.10",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "@types/express": "^4.17.21",
    "@types/node": "^20.8.2",
    "@types/pg": "^8.10.7",
    "ts-node-dev": "^2.0.0"
  }
}
